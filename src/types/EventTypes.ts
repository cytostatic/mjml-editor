export enum Events {
    Logging = 'mjml-editor.logging',
    GetEditorContent = 'mjml-editor.getEditorContent',
    FetchFileContent = 'mjml-editor.fetchFileContent',
    FetchImage = 'mjml-editor.fetchImage',
}